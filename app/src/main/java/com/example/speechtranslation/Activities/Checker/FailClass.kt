package com.example.speechtranslation.Activities.Checker

class FailClass {
    fun failingMethod() {
        val number: Int = "This is a string" // Type mismatch error
        println(number)
    }
}